---
'@simplepg/frontend': minor
---

Add a side-by-side markdown preview toggle in the editor and remember its state between visits. Improve the editing layout by expanding the editor in preview mode and aligning editor sizing with the rendered preview for better scroll sync.
