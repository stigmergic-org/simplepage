{
  "name": "root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "workspaces": [
    "packages/*",
    "frontend"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.5"
  }
}