{
  "name": "root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "stage": "cd frontend; pnpm run stage",
    "lint": "pnpm -r lint"
  },
  "workspaces": [
    "packages/*",
    "frontend"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.5"
  }
}