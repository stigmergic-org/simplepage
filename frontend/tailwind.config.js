// frontend/tailwind.config.ts (optional, minimal)
export default {
  content: ["./public/index.html", "./src/**/*.{js,jsx,ts,tsx}"],
  // No plugins here when using @plugin in CSS
  // Remove darkMode to avoid fighting data-theme
};